name: Manager Worker

on:
  workflow_dispatch:

# PAUSA: orquestracao automatica (ops) e dispatch automatico do Copilot.
# O workflow permanece disponivel somente para execucao manual.

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  manager:
    name: Manager — paused
    runs-on: ubuntu-latest
    steps:
      - run: echo "Manager Worker paused by ControleOnline/agents-mcp#201"

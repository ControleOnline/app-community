name: Technical Documenter (centralizado agents-mcp)

on:
  workflow_dispatch:

# PAUSA CTO ControleOnline/agents-mcp#201 (2026-08-29)
# Trigger original desativado: push em master, staging.
# Este workflow atribuia copilot-swe-agent[bot].

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  technical-documenter:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Technical Documenter Copilot dispatch paused by ControleOnline/agents-mcp#201"

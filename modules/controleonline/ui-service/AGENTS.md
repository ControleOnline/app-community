# ui-service

- A home deve ser orientada por `theme.menus` e reaproveitar `AppMenuGrid` como entrada principal.
- O primeiro fluxo operacional do app e `LabelsPage`, entregue pelo menu `Criador de etiquetas`.
- O publico principal sao `employee` e `owner`; nao assumir comportamento de `MANAGER`.
- Nao incluir checklist no escopo deste modulo.

checks:
  javascript: true

build:
  nodes:
    analysis:
      project_setup:
        override: true
      tests:
        override:
          - js-scrutinizer-run

filter:
  excluded_paths:
    - "android/*"
    - "dist/*"
    - "dist-web-check/*"
    - "node_modules/*"
